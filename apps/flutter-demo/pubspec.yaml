name: flutter_demo
description: Fulfillment Design System — Flutter demo (Brand A + Brand B mockups)
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  formbricks_flutter: ^0.0.7
  fulfillment_design_system:
    path: ../../packages/ui-flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
